<!-- <h1>Users#show</h1>
<p>Find me in app/views/users/show.html.erb</p> -->
<div class="container">
	<!--  button to nav back to browse -->
	<% if current_user == @user %>
		<%= render 'add_games_button' %>
	<% end %>



	<ul>
		<%= render partial: 'games/game', collection: @games %>
	</ul>

	<h2>Friends!</h2>
	<ul>
		<%= render partial: 'partials/friend', collection: @friends %>
	</ul>

</div>
